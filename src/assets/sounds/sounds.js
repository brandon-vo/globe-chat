const sounds = {
    button: require('./button.wav').default,
    click: require('./click.wav').default,
    message: require('./message.wav').default,
    signOut: require('./signOut.wav').default,
    switch: require('./switch.wav').default,
}

export default sounds;